# {{ class_name }}

Generated by AgentForge on {{ timestamp }}.

## Quick Start

```bash
cp .env.example .env
python main.py
```

## Endpoints

- `GET /` - basic agent metadata
- `GET /health` - health check
- `POST /chat` - send a chat request
- `POST /clear` - clear in-memory conversation history

## Development

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pytest
```
